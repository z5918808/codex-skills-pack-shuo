interface:
  display_name: "Lock In"
  short_description: "Nudge the task back to its main objective."
  default_prompt: "Use $recenter to lock onto the main objective and resume the next useful action."
