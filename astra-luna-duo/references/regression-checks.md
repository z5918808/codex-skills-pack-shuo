# Compatibility routing regressions

- This alias runs the canonical invocation decision gate and does not force the historical fixed roster.
- Current explicit model/role overrides bind their actual source and preserve the user's Main; the filename alone is insufficient.
- Luna retains sustained execution and huge digestion. Sol's bounded intervention returns the remaining work; Astra is triggered, not always created.
- Existing legacy pinned paths, authority, identities, direct deliveries and unresolved effects remain in force until safe reconciliation. Maintenance never changes a live goal or dispatches roles.
- A fresh sustained repo invocation uses the shared `TASK_GOAL.md`; Main prunes stale generations before dispatch without a separate cleanup request. A pinned root waits for consumer reconciliation, and existing `DUO_ASTRA_GOAL.md` migrates only at that boundary.
- Apply [canonical semantic scenarios](../../trio-long-running/references/regression-checks.md) and the executable routing-gate tests for fresh work. Legacy handoff/reporting references remain only for their existing pinned consumers.
