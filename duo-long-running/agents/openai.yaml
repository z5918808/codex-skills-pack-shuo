interface:
  display_name: "Duo Long Running"
  short_description: "Worker/Reviewer with evidence-gated acceptance"
  default_prompt: "Use $duo-long-running with this chat as [Reviewer] and a separate [Worker], require an evidence packet plus a ship, fix-first, or rethink verdict for acceptance, and never use cross-task waits or polling."
