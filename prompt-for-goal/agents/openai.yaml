interface:
  display_name: "Goal Compiler"
  short_description: "編譯精短、可貼、可執行的 goal prompt。"
  default_prompt: "Use $prompt-for-goal to produce one concise executable goal block with the outcome, current authority, a three-step roadmap, and only essential safety and stop rules; omit construction history and repeated repository contracts."
