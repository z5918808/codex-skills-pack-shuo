interface:
  display_name: "Bigbots Deploy"
  short_description: "Route GPT-5.6 review and execution sidecars."
  default_prompt: "Use $bigbots-deploy to run up to three GPT-5.6 sidecars with Sol high for review or Terra high for approved execution."
